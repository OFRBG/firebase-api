// @format
import {add, fetch, remove} from '../../../utils';
import {schema} from '../type';

export const collectionName = '{{ collectionName }}';

/**
 * Fetch the data for {{ properCase name }} from storage
 */
export const get{{ properCase name }} = (idPath = collectionName) =>
  async (root: any, args: any, context: any) =>
    fetch(collectionName, root, args, idPath);

/**
 * Set the data for {{ properCase name }} to a new doc
 */
export const set{{ properCase name }} = async (root: any, args: any, context: any) =>
  add(collectionName, schema, root, args);

/**
 * Remove a list of docs of {{ properCase name }}
 */
export const remove{{ properCase name}} = async (root: any, args: any, context: any) =>
  remove(collectionName, root, args);
