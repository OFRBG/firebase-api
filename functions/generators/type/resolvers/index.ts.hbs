// @format
import {model as type} from '../type';
import { {{~ properCase name }}Input as input} from '../inputTypes';

import {set{{ properCase name }} as resolve} from './resolvers';

const args = {
  inputObject: {
    type: input,
  },
};

export const setter = {type, args, resolve};
